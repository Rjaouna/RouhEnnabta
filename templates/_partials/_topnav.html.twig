<style>
.topbar {
  position: relative;
  z-index: 1055;
  background-color: #fdfbf7; /* très léger, naturel */
  border-bottom: 1px solid #eee6d8;
  font-size: 0.85rem;
  color: #6c757d;
}

.topbar a,
.topbar span {
  color: #6c757d;
}

.topbar a:hover {
  color: #c2a05c; /* primary */
  text-decoration: none;
}

/* Icônes */
.topbar i {
  font-size: 1rem;
  opacity: 0.85;
}

/* Dropdown support */
.support-dropdown > a {
  font-weight: 500;
}

.support-dropdown .dropdown-menu {
  min-width: 220px;
  margin-top: .6rem;
  border-radius: 8px;
  border: 1px solid #eee6d8;
}

.support-dropdown .dropdown-item {
  font-size: .85rem;
  color: #6c757d;
}

.support-dropdown .dropdown-item i {
  font-size: 1rem;
}

.support-dropdown .dropdown-item:hover {
  background-color: #f7f4ee;
  color: #c2a05c;
}

/* Accent icônes spécifiques */
.topbar .bi-headset {
  color: #c2a05c;
}

.topbar .bi-geo-alt {
  color: #8FB397;
}

.topbar .bi-patch-check {
  color: #c2a05c;
}

/* Réseaux sociaux */
.topbar .social a i {
  color: #8FB397;
  transition: color .2s ease, transform .2s ease;
}

.topbar .social a:hover i {
  color: #c2a05c;
  transform: translateY(-1px);
}

</style>

<div class="d-none d-lg-block topbar">
	<div
		class="container d-flex justify-content-between align-items-center py-1">

		{# GAUCHE #}
		<div class="d-flex align-items-center gap-4">

			<div class="support-dropdown dropdown">
				<a href="#" class="d-flex align-items-center" data-bs-toggle="dropdown">
					<i class="bi bi-headset me-2"></i>
					Support & assistance
				</a>

				<ul class="dropdown-menu shadow-sm">
					<li>
						<a class="dropdown-item d-flex align-items-center gap-2" href="https://wa.me/212665922104" target="_blank">
							<i class="bi bi-whatsapp text-success"></i>
							WhatsApp
						</a>
					</li>
					<li>
						<a class="dropdown-item d-flex align-items-center gap-2" href="{{ path('app_contact') }}">
							<i class="bi bi-envelope"></i>
							Nous contacter
						</a>
					</li>
					<li>
						<a class="dropdown-item d-flex align-items-center gap-2" href="#">
							<i class="bi bi-question-circle"></i>
							Foire aux questions
						</a>
					</li>
				</ul>
			</div>

		

			<span class="d-flex align-items-center">
				<i class="bi bi-patch-check me-2"></i>
				Plateforme officielle Terroir du Maroc
			</span>

		</div>

		{# DROITE #}
		<div class="social d-flex align-items-center gap-3">
			<a href="#" aria-label="Instagram">
				<i class="bi bi-instagram fs-5"></i>
			</a>
			<a href="#" aria-label="Facebook">
				<i class="bi bi-facebook fs-5"></i>
			</a>
		</div>

	</div>
</div>

